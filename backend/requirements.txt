fastapi==0.136.1
uvicorn[standard]==0.34.0
motor==3.7.0
pymongo==4.10.1
PyJWT==2.13.0
cryptography==48.0.1
bcrypt==4.2.1
python-multipart==0.0.31
openai==1.68.2
anthropic==0.49.0
google-generativeai==0.8.3
pydantic-settings==2.7.1
google-auth[requests]==2.38.0
slowapi==0.1.9
redis==5.2.1
email-validator==2.2.0
boto3==1.35.49

# ---- transitive CVE floor pins ------------------------------------------------
# starlette: GHSA-7f5h-v6xp-fcq8 (Range header DoS) + GHSA-2c2j-9gv5-cj73 (upload
# event-loop block) + PYSEC-2026-161 + CVE-2026-54283 (form limits ignored for
# application/x-www-form-urlencoded, DoS). fastapi<0.133.0 capped starlette<0.51.0
# so the fix required upgrading fastapi to >=0.133.0; using 0.136.1.
starlette>=1.3.1

# urllib3: GHSA-qccp-gfcp-xxvc + GHSA-mf9v-mfxr-j63j (redirect header leak +
# Brotli decompression bomb). Pulled in by openai / anthropic / google-auth SDKs.
urllib3>=2.7.0

# requests: GHSA-gc5v-m9x4-r6x2 (predictable temp file in extract_zipped_paths).
# Not exploitable via current code paths, but pin for defence in depth.
# Pulled in by google-auth[requests].
requests>=2.33.0

# python-dotenv: GHSA-mf9w-mj56-hr94 (symlink follow in set_key/unset_key).
# Not called at runtime; only affects local dev .env writes.
python-dotenv>=1.2.2

# ---- disputed / no-fix CVE suppressions --------------------------------------
# PyJWT PYSEC-2025-183: "weak encryption" advisory — DISPUTED by PyJWT maintainers.
# The finding is that callers may supply short HMAC keys; the library does not enforce
# a minimum key length.  Not applicable here: the backend uses RS256 with a 2048-bit
# RSA key pair, so short HMAC key length is irrelevant.
# No fixed version exists; the advisory is suppressed via --ignore-vuln in pip-audit.
