# CVE-2024-21907: Newtonsoft.Json in react-native-fs/windows/RNFS.Net46
# This is a Windows-platform NuGet dependency inside node_modules.
# The app targets Android and iOS only — the Windows code path is never compiled or shipped.
CVE-2024-21907

# CVE-2026-14257 / GHSA-mh99-v99m-4gvg: brace-expansion DoS (< 5.0.8)
# Transitive dependency of eslint and jest — dev tooling only.
# Not included in any shipped binary (Android APK or iOS IPA).
CVE-2026-14257

# CVE-2025-71329, CVE-2025-71330 — image-size DoS via crafted image buffers.
# No patched release exists upstream yet. image-size is a transitive dependency
# of Metro (via expo/@react-native/metro-config) and only runs at build time —
# it never ships in the app bundle. See frontend-app/yarn.lock and check.sh's
# yarn audit IGNORED_IDS (1138808, 1138809) for the same exception.
CVE-2025-71329
CVE-2025-71330
